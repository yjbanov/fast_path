import 'package:fast_path/fast_path.dart';
import 'package:test/test.dart';

void main() {
  group('fast_path', () {
    test('library loads', () {
      // Placeholder smoke test. Ensures the library imports cleanly.
      // Replace with real coverage as the public API lands.
      expect(true, isTrue);
    });
  });
}
